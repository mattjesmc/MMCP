# TODO.md - moved to the workbench archive

This file was the toolkit's **internal work list** - numbered threads (`1.9`, `2.8`, `3.2`, `4.5`,
...) that **39 files in this tree still cite as provenance**, mostly probes explaining why they
exist. It is a development backlog, not documentation of the shipped software.

**It lives in the workbench's private archive repository** (`mcmodding-archive`, at this same path),
together with `RELEASE_1.md` and `docs/archive/TODO_SHIPPED.md`.

**This stub stays so those 39 citations still resolve to something that explains itself** rather than
to a missing file - see the note in `RELEASE_1.md` beside it.

**What replaces it for a reader here:** `RELEASE.md` sections 4 and 5 - what was built, what was
deliberately descoped, and what hygiene is still open, with the recommendation and the decision for
each.
